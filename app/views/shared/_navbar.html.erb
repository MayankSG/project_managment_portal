<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <% if user_signed_in?%>
        <li class="nav-item active">
          <%= link_to "Home", root_path, class:"nav-link active" %>
        </li>
        <li class="nav-item active">
          <%= link_to 'Log out', destroy_user_session_path, data: { turbo_method: :delete}, class:"nav-link active" %>
        </li>
      <% else %>
        <li class="nav-item active">
          <%= link_to "Log in", new_session_path(resource_name), class:"nav-link active" %>
        </li>
        <li>
          <%= link_to "Sign up", new_registration_path(resource_name), class:"nav-link active" %>
        </li>
      <% end %>
    </ul>
  </div>
    <ul class="navbar-nav ml-auto">
    <% if user_signed_in?%>
      <li class="nav-item active" >
        <%= link_to current_user.email, edit_user_registration_path(current_user), class:"nav-link active" %>
      </li>
    <% else %>
      <li class="nav-item active" >
        <%= link_to "Forgot your password?", new_password_path(resource_name), class:"nav-link active" %>
      </li>
    <% end %>
    </ul>
</nav>